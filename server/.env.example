# Development defaults
PORT=17000
CORS_ORIGIN=http://localhost:3000

# Production example (separate domains)
# PORT=17000
# CORS_ORIGIN=https://app.example.com
# Persistent data file path (use on managed hosts with writable volumes)
# If unset, the server defaults to ./data.json next to main.ts
# Example for production (Railway/Render/Fly/Koyeb volumes):
# DATA_FILE=/data/data.json